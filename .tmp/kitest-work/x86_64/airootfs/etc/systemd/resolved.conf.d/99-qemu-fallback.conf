# When per-link DNS (e.g. DHCP) is missing or flaky, try public resolvers.
# Does not override per-link DNS when DHCP works. Avoids 10.0.2.3 on bare metal
# (QEMU user NAT DNS is still delivered via DHCP when using virtio + user netdev).

[Resolve]
FallbackDNS=1.1.1.1 9.9.9.9
