[main]
plugins=keyfile

# QEMU user (-netdev user) / slirp: captive-portal checks often fail or flap; disable periodic connectivity tests.
[connectivity]
interval=0

[device]
wifi.backend=wpa_supplicant
