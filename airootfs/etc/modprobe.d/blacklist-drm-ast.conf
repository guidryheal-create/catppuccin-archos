# ASPEED AST (BMC / server VGA) — not needed on most laptops/desktops. Loading drm.ast
# in the initramfs triggers mkinitcpio warnings about firmware blobs that are optional
# or renamed in linux-firmware; blacklisting avoids noisy builds and useless module loads.
# If you actually use this GPU, remove this file and rebuild the ISO.
blacklist ast
