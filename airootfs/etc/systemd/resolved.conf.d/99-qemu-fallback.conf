# QEMU user networking (-netdev user): slirp usually provides DNS at 10.0.2.3 via DHCP.
# If uplink DNS is missing or flaky, systemd-resolved can still try this fallback.
# Does not override per-link DNS when DHCP works on real hardware.

[Resolve]
FallbackDNS=10.0.2.3
